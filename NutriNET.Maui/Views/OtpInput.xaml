<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NutriNET.Maui.Views.OtpInput">
    <Grid ColumnDefinitions="*, *,*,*, *, *" ColumnSpacing="8">
        <Border Grid.Column="0" StrokeShape="RoundRectangle 8" Stroke="{StaticResource Gray400}" StrokeThickness="1.5" HeightRequest="56" WidthRequest="44" HorizontalOptions="Center">
            <Entry x:Name="Entry0" MaxLength="1" Keyboard="Numeric" HorizontalTextAlignment="Center" FontSize="22" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="White" TextChanged="OnTextChanged" Focused="OnFocused" />
        </Border>
        <Border Grid.Column="1" StrokeShape="RoundRectangle 8" Stroke="{StaticResource Gray400}" StrokeThickness="1.5" HeightRequest="56" WidthRequest="44" HorizontalOptions="Center">
            <Entry x:Name="Entry1" MaxLength="1" Keyboard="Numeric" HorizontalTextAlignment="Center" FontSize="22" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="White" TextChanged="OnTextChanged" Focused="OnFocused" />
        </Border>
        <Border Grid.Column="2" StrokeShape="RoundRectangle 8" Stroke="{StaticResource Gray400}" StrokeThickness="1.5" HeightRequest="56" WidthRequest="44" HorizontalOptions="Center">
            <Entry x:Name="Entry2" MaxLength="1" Keyboard="Numeric" HorizontalTextAlignment="Center" FontSize="22" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="White" TextChanged="OnTextChanged" Focused="OnFocused" />
        </Border>
        <Border Grid.Column="3" StrokeShape="RoundRectangle 8" Stroke="{StaticResource Gray400}" StrokeThickness="1.5" HeightRequest="56" WidthRequest="44" HorizontalOptions="Center">
            <Entry x:Name="Entry3" MaxLength="1" Keyboard="Numeric" HorizontalTextAlignment="Center" FontSize="22" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="White" TextChanged="OnTextChanged" Focused="OnFocused" />
        </Border>
        <Border Grid.Column="4" StrokeShape="RoundRectangle 8" Stroke="{StaticResource Gray400}" StrokeThickness="1.5" HeightRequest="56" WidthRequest="44" HorizontalOptions="Center">
            <Entry x:Name="Entry4" MaxLength="1" Keyboard="Numeric" HorizontalTextAlignment="Center" FontSize="22" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="White" TextChanged="OnTextChanged" Focused="OnFocused" />
        </Border>
        <Border Grid.Column="5" StrokeShape="RoundRectangle 8" Stroke="{StaticResource Gray400}" StrokeThickness="1.5" HeightRequest="56" WidthRequest="44" HorizontalOptions="Center">
            <Entry x:Name="Entry5" MaxLength="1" Keyboard="Numeric" HorizontalTextAlignment="Center" FontSize="22" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="White" TextChanged="OnTextChanged" Focused="OnFocused" />
        </Border>
    </Grid>
</ContentView>
